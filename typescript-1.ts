// let message = "Hello";

// message.toLowerCase();

// message();
// console.log(message.toLowerCase())

function greet(person: string, date: number): string {
  // console.log(`Hello ${person}, today is ${date}!`)
  // ;
  return `Helllo ${person}, today is ${date}!`;
}

greet("Himansu", 22);

// function printId(id: number | string) {
//   console.log(id.toUpperCase());
// }

// function printId(id: number | string) {
//   if (typeof id === "string") {
//     console.log(id.toUpperCase());
//   } else {
//     console.log(id);
//   }
// }

// // printId(22)
// printId("22");

// type Window = {
//   title: string;
// }

// type Window = {
//   ts: TypeScriptAPI;
// }
